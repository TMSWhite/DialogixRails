<h1>Listing instrument_contents</h1>

<table>
  <tr>
  </tr>

<% for instrument_content in @instrument_contents %>
  <tr>
    <td><%= link_to 'Show', instrument_content %></td>
    <td><%= link_to 'Edit', edit_instrument_content_path(instrument_content) %></td>
    <td><%= link_to 'Destroy', instrument_content, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New instrument_content', new_instrument_content_path %>
